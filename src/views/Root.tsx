import Routes from 'routes/Routes';
import MainTemplate from 'templates/MainTemplate';

const Root = () => {
  return (
    <MainTemplate>
      <Routes />
    </MainTemplate>
  );
};

export default Root;
